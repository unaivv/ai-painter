{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // \"\"'); echo \"$f\" | grep -q 'package\\.json$' && npx --yes autoskills 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Discovering skills for new dependencies..."
          }
        ]
      }
    ]
  }
}
